# PyTorch is NOT listed here — install it first with CUDA 12.8 support:
#
#   Linux / Windows (CMD or PowerShell):
#     pip install torch==2.8.0 torchaudio==2.8.0 torchvision==0.23.0 \
#         --index-url https://download.pytorch.org/whl/cu128
#
# Also install ffmpeg and add it to PATH:
#   Linux:   sudo apt install ffmpeg
#   Windows: winget install Gyan.FFmpeg

whisperx==3.8.5
faster-whisper==1.2.1
ctranslate2==4.7.1
pyannote.audio==4.0.4
# whisperX 3.8.5 requires huggingface-hub<1.0.0; pin to latest 0.x
huggingface-hub==0.36.2
transformers>=4.48.0
torchcodec==0.7.0
omegaconf==2.3.0
nltk>=3.9.1
numpy>=2.1.0
pandas>=2.2.3
# triton is a Linux/macOS-only torch dependency — not available on Windows, skip it
soundfile==0.13.1
python-dotenv>=1.0.0
mcp[cli]>=1.0.0
watchdog>=4.0.0
